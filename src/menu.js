const utils = require('./utils');

const menu = {
  title: 'Main Menu',
  items: [
    { id: 'help', label: 'Help' },
    { id: 'home', label: 'Home' },
    { id: 'about', label: 'About' }
  ]
};

module.exports = menu;